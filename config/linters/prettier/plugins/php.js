/**
 * --------------------------------------------------------------------------
 *   Linters → Prettier → Plugins → PHP
 *   @see https://github.com/prettier/plugin-php
 * --------------------------------------------------------------------------
*/

module.exports = {
  plugins: ['@prettier/plugin-php']
};
