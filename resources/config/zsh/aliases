# --------------------------------------------------------------------------
#   ZSH → Aliases
# --------------------------------------------------------------------------

#   A
# ------------------------------------------------------------
#   B
# ------------------------------------------------------------

#   C
# ------------------------------------------------------------
# Clear
alias c="clear"

# Make copy interactive and verbose.
alias cp="cp -i"


#   D
# ------------------------------------------------------------

#   E
# ------------------------------------------------------------
# Always enable colored `grep` output.
alias egrep="egrep --color=auto"


#   F
# ------------------------------------------------------------
# Always enable colored `grep` output.
alias fgrep="fgrep --color=auto"


#   G
# ------------------------------------------------------------
# Always enable colored `grep` output.
alias grep="grep --color=auto"


#   H
# ------------------------------------------------------------
#   I
# ------------------------------------------------------------
#   J
# ------------------------------------------------------------
#   K
# ------------------------------------------------------------


#   L
# ------------------------------------------------------------
# Always list everything in a directory.
alias ls="exa -alh --git --icons"


# Always list everything in a directory and subdirectories.
alias lsr="ls -lARFh --color"


#   M
# ------------------------------------------------------------
# Make directory then change into the newly-created directory.
function mkcd() {
  mkdir -p $1 && cd $1
}

# Make a directory, including any parent directories that need to be created.
# alias mkdir="mkdir -p"

# Make move interactive and verbose.
alias mv="mv -i --verbose"


#   N
# ------------------------------------------------------------


#   O
# ------------------------------------------------------------
# Open current working directory with Finder.
# @usage o
# @returns Finder window of current directory.
function o {
	if [[ $# -eq 0 ]] then
		open .;
	else
		open "$@";
	fi
}

#   P
# ------------------------------------------------------------
#   Q
# ------------------------------------------------------------


#   R
# ------------------------------------------------------------
# Make remove interactive and verbose.
alias rm="rm -iv"


#   S
# ------------------------------------------------------------
#   T
# ------------------------------------------------------------
#   U
# ------------------------------------------------------------
#   V
# ------------------------------------------------------------
#   W
# ------------------------------------------------------------
#   X
# ------------------------------------------------------------
#   Y
# ------------------------------------------------------------
#   Z
# ------------------------------------------------------------
